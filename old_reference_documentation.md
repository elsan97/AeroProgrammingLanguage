# Aero Language - Полный справочник команд
# Синтаксис: {команда}параметры{|}

## 📚 КАТЕГОРИИ КОМАНД

### 1. 🎯 БАЗОВЫЕ КОМАНДЫ (100+)
```
{Aero}код{|}                      - Основная команда Aero (начало программы)
{echo}текст{|}                    - Вывод текста
{print}значение{|}                - Печать значения
{input}переменная{|}              - Ввод с клавиатуры
{clear}{|}                        - Очистка экрана
{pause}{|}                        - Пауза
{exit}код{|}                      - Выход из программы
{cls}{|}                          - Очистка консоли
{type}текст{|}                    - Вывод без перевода строки
{println}текст{|}                 - Вывод с переводом строки
{error}сообщение{|}               - Вывод ошибки
{warning}сообщение{|}             - Вывод предупреждения
{info}сообщение{|}                - Вывод информации
{debug}данные{|}                  - Отладочный вывод
{trace}метка{|}                   - Трассировка
{assert}условие{|}                - Проверка утверждения
```

### 2. 📊 ПЕРЕМЕННЫЕ И ТИПЫ (150+)
```
{int}#имя = значение{|}           - Целое число
{str}#имя = "текст"{|}            - Строка
{float}#имя = 3.14{|}             - Число с плавающей точкой
{bool}#имя = true{|}              - Логическое значение
{array}#имя = [1,2,3]{|}          - Массив
{dict}#имя = {ключ:значение}{|}   - Словарь
{set}#имя = {1,2,3}{|}            - Множество
{null}#имя{|}                     - Null значение
{const}#имя = значение{|}         - Константа
{global}#имя = значение{|}        - Глобальная переменная
{local}#имя = значение{|}         - Локальная переменная
{static}#имя = значение{|}        - Статическая переменная
{var}#имя = значение{|}           - Объявление переменной
{let}#имя = значение{|}           - Блочная переменная
```

### 3. 🧮 МАТЕМАТИКА (200+)
```
{add}a b{|}                       - Сложение
{sub}a b{|}                       - Вычитание
{mul}a b{|}                       - Умножение
{div}a b{|}                       - Деление
{mod}a b{|}                       - Остаток от деления
{pow}a b{|}                       - Возведение в степень
{sqrt}число{|}                    - Квадратный корень
{sin}угол{|}                      - Синус
{cos}угол{|}                      - Косинус
{tan}угол{|}                     - Тангенс
{abs}число{|}                     - Модуль
{round}число{|}                   - Округление
{floor}число{|}                   - Округление вниз
{ceil}число{|}                   - Округление вверх
{log}число{|}                     - Логарифм
{exp}число{|}                     - Экспонента
{random}мин макс{|}               - Случайное число
{pi}{|}                           - Число π
{e}{|}                            - Число e
```

### 4. 🧠 ЛОГИКА И УСЛОВИЯ (100+)
```
{if}условие{|}                    - Условие
{else}{|}                         - Иначе
{elif}условие{|}                  - Иначе если
{switch}значение{|}               - Переключатель
{case}значение{|}                 - Вариант
{default}{|}                      - По умолчанию
{and}a b{|}                       - Логическое И
{or}a b{|}                        - Логическое ИЛИ
{not}значение{|}                  - Логическое НЕ
{eq}a b{|}                        - Равно
{neq}a b{|}                       - Не равно
{gt}a b{|}                        - Больше
{lt}a b{|}                        - Меньше
{gte}a b{|}                       - Больше или равно
{lte}a b{|}                       - Меньше или равно
{xor}a b{|}                       - Исключающее ИЛИ
{nand}a b{|}                      - И-НЕ
{nor}a b{|}                       - ИЛИ-НЕ
```

### 5. 🔄 ЦИКЛЫ (50+)
```
{for}инициализация условие шаг{|} - Цикл for
{while}условие{|}                 - Цикл while
{dowhile}условие{|}               - Цикл do-while
{foreach}элемент in коллекция{|}  - Цикл foreach
{break}{|}                        - Прервать цикл
{continue}{|}                     - Продолжить цикл
{loop}количество{|}               - Цикл с счётчиком
{repeat}количество{|}             - Повторить N раз
{iterate}коллекция{|}             - Итерация
{range}начало конец шаг{|}        - Диапазон
```

### 6. 🏗️ ФУНКЦИИ (100+)
```
{func}имя(параметры){|}           - Объявление функции
{return}значение{|}               - Возврат значения
{call}имя(аргументы){|}           - Вызов функции
{param}имя значение{|}            - Параметр функции
{arg}индекс{|}                    - Аргумент
{lambda}(параметры)=>выражение{|} - Лямбда-функция
{closure}переменные{|}            - Замыкание
{callback}функция{|}              - Функция обратного вызова
{recursive}функция{|}             - Рекурсивная функция
{generator}функция{|}             - Генератор
{async}функция{|}                 - Асинхронная функция
{await}выражение{|}               - Ожидание
{promise}значение{|}              - Обещание
```

### 7. 📁 ФАЙЛЫ И СИСТЕМА (200+)
```
{file}путь режим{|}               - Открытие файла
{read}файл{|}                     - Чтение файла
{write}файл данные{|}             - Запись в файл
{append}файл данные{|}            - Добавление в файл
{delete}файл{|}                   - Удаление файла
{copy}источник цель{|}            - Копирование файла
{move}источник цель{|}            - Перемещение файла
{rename}старое новое{|}           - Переименование
{mkdir}папка{|}                   - Создание папки
{rmdir}папка{|}                   - Удаление папки
{cd}путь{|}                       - Смена директории
{ls}путь{|}                       - Список файлов
{dir}путь{|}                      - Список каталога
{pwd}{|}                          - Текущая директория
{find}паттерн{|}                  - Поиск файлов
{grep}текст файл{|}               - Поиск в файле
{cat}файл{|}                      - Вывод содержимого
{head}файл строки{|}              - Начало файла
{tail}файл строки{|}              - Конец файла
```

### 8. ⚙️ ПРОЦЕССЫ И СЕТЬ (150+)
```
{run}команда{|}                   - Запуск команды
{exec}программа{|}                - Выполнение программы
{shell}команда{|}                 - Команда shell
{cmd}команда{|}                   - Команда CMD
{powershell}скрипт{|}             - PowerShell
{kill}pid{|}                      - Завершить процесс
{pid}имя{|}                       - ID процесса
{ps}{|}                           - Список процессов
{net}операция{|}                  - Сетевые операции
{http}метод url{|}                - HTTP запрос
{socket}хост порт{|}              - Сокет
{connect}хост порт{|}             - Подключение
{listen}порт{|}                   - Прослушивание
{send}данные{|}                   - Отправка данных
{recv}{|}                         - Получение данных
{ping}хост{|}                     - Ping
{traceroute}хост{|}               - Traceroute
{curl}url{|}                      - CURL запрос
{wget}url{|}                      - Загрузка файла
```

### 9. 🎨 ГРАФИКА И GUI (300+)
```
{window}ширина высота название{|} - Создание окна
{button}текст x y{|}              - Кнопка
{label}текст x y{|}               - Метка
{textbox}текст x y{|}             - Текстовое поле
{canvas}ширина высота{|}          - Холст
{draw}команда{|}                  - Рисование
{line}x1 y1 x2 y2{|}              - Линия
{rect}x y ширина высота{|}        - Прямоугольник
{circle}x y радиус{|}             - Круг
{image}путь x y{|}                - Изображение
{color}r g b{|}                   - Цвет
{font}имя размер{|}               - Шрифт
{menu}пункты{|}                   - Меню
{toolbar}кнопки{|}                - Панель инструментов
{statusbar}текст{|}               - Строка состояния
{scrollbar}ориентация{|}          - Полоса прокрутки
{slider}мин макс{|}               - Слайдер
{checkbox}текст{|}                - Флажок
{radiobutton}текст{|}             - Радиокнопка
{listbox}элементы{|}              - Список
{combobox}элементы{|}             - Выпадающий список
{treeview}узлы{|}                 - Дерево
{table}столбцы строки{|}          - Таблица
```

### 10. 🎮 ИГРЫ И МУЛЬТИМЕДИА (200+)
```
{game}название разрешение{|}      - Создание игры
{sprite}путь x y{|}               - Спрайт
{animate}спрайт кадры{|}          - Анимация
{sound}путь{|}                    - Звук
{music}путь{|}                    - Музыка
{play}медиа{|}                    - Воспроизведение
{stop}медиа{|}                    - Остановка
{volume}уровень{|}                - Громкость
{keyboard}код{|}                  - Обработка клавиатуры
{mouse}кнопка x y{|}              - Обработка мыши
{joystick}ось кнопка{|}           - Джойстик
{camera}x y z{|}                  - Камера
{light}тип позиция{|}             - Освещение
{texture}путь{|}                  - Текстура
{model}путь{|}                    - 3D модель
{particle}тип количество{|}       - Частицы
{physics}объект{|}                - Физика
{collision}объект1 объект2{|}     - Столкновение
{ai}поведение{|}                  - Искусственный интеллект
{pathfinding}начало конец{|}      - Поиск пути
```

### 11. 🗄️ БАЗЫ ДАННЫХ (100+)
```
{db}тип подключение{|}            - Подключение к БД
{sql}запрос{|}                    - SQL запрос
{select}поля таблица{|}           - SELECT
{insert}таблица значения{|}       - INSERT
{update}таблица значения{|}       - UPDATE
{delete}таблица условие{|}        - DELETE
{where}условие{|}                 - WHERE
{join}таблица1 таблица2{|}        - JOIN
{table}имя столбцы{|}             - Создание таблицы
{index}таблица столбец{|}         - Индекс
{transaction}{|}                  - Транзакция
{commit}{|}                       - Подтверждение
{rollback}{|}                     - Откат
{query}запрос{|}                  - Запрос
{fetch}результат{|}               - Получение результата
{row}индекс{|}                    - Строка
{column}имя{|}                    - Столбец
```

### 12. 🌐 ВЕБ И API (150+)
```
{web}адрес порт{|}                - Веб-сервер
{html}код{|}                      - HTML
{css}стили{|}                     - CSS
{js}код{|}                       - JavaScript
{api}эндпоинт{|}                  - API
{json}данные{|}                   - JSON
{xml}данные{|}                    - XML
{rest}метод url{|}                - REST запрос
{get}url{|}                       - GET запрос
{post}url данные{|}               - POST запрос
{put}url данные{|}                - PUT запрос
{patch}url данные{|}              - PATCH запрос
{delete}url{|}                    - DELETE запрос
{header}ключ значение{|}          - HTTP заголовок
{cookie}имя значение{|}           - Cookie
{session}данные{|}                - Сессия
{auth}логин пароль{|}             - Аутентификация
{oauth}провайдер{|}               - OAuth
{jwt}токен{|}                     - JWT токен
{websocket}url{|}                 - WebSocket
```

### 13. 🔐 БЕЗОПАСНОСТЬ (100+)
```
{encrypt}данные ключ{|}           - Шифрование
{decrypt}данные ключ{|}           - Дешифрование
{hash}данные алгоритм{|}          - Хеширование
{sign}данные ключ{|}              - Подпись
{verify}данные подпись ключ{|}    - Проверка подписи
{auth}логин пароль{|}             - Аутентификация
{login}учётные данные{|}          - Вход
{logout}{|}                       - Выход
{token}данные{|}                  - Токен
{password}пароль{|}               - Пароль
{key}тип длина{|}                 - Ключ
{certificate}путь{|}              - Сертификат
{ssl}сертификат ключ{|}           - SSL
{tls}версия{|}                    - TLS
{firewall}правило{|}              - Фаервол
{vpn}подключение{|}               - VPN
{antivirus}сканирование{|}        - Антивирус
{backup}данные{|}                 - Резервная копия
{recovery}данные{|}               - Восстановление
```

### 14. ⚡ ПАРАЛЛЕЛИЗМ (50+)
```
{thread}функция{|}                - Поток
{async}функция{|}                 - Асинхронная функция
{await}выражение{|}               - Ожидание
{promise}значение{|}              - Обещание
{mutex}имя{|}                     - Мьютекс
{semaphore}значение{|}            - Семафор
{lock}ресурс{|}                   - Блокировка
{unlock}ресурс{|}                 - Разблокировка
{parallel}задачи{|}               - Параллельное выполнение
{concurrent}потоки{|}             - Конкурентность
{sync}операция{|}                 - Синхронизация
{race}промисы{|}                  - Гонка промисов
{all}промисы{|}                   - Все промисы
{any}промисы{|}                   - Любой промис
{pool}размер{|}                   - Пул потоков
{queue}задачи{|}                  - Очередь задач
{channel}буфер{|}                 - Канал
{worker}скрипт{|}                 - Воркер
{cluster}процессы{|}              - Кл��стер
{fork}процесс{|}                  - Форк процесса
```

### 15. 💻 СИСТЕМНОЕ ПРОГРАММИРОВАНИЕ (100+)
```
{mem}адрес размер{|}              - Память
{ptr}адрес{|}                     - Указатель
{alloc}размер{|}                  - Выделение памяти
{free}адрес{|}                    - Освобождение памяти
{asm}код{|}                       - Встроенный ассемблер
{reg}регистр значение{|}          - Регистр
{interrupt}номер{|}               - Прерывание
{port}номер значение{|}           - Порт ввода-вывода
{bios}функция{|}                  - BIOS вызов
{kernel}функция{|}                - Вызов ядра
{driver}устройство{|}             - Драйвер
{device}имя{|}                    - Устройство
{io}операция{|}                   - Ввод-вывод
{dma}канал{|}                     - Прямой доступ к памяти
{irq}номер{|}                     - Запрос прерывания
{cpu}инструкция{|}                - Инструкция процессора
{gpu}команда{|}                   - Команда GPU
{mmu}таблица{|}                   - Блок управления памятью
{cache}операция{|}                - Кэш
{pipeline}стадии{|}               - Конвейер
```

### 16. 📅 ДАТА И ВРЕМЯ (50+)
```
{date}формат{|}                   - Дата
{time}формат{|}                   - Время
{datetime}формат{|}               - Дата и время
{timer}интервал{|}                - Таймер
{sleep}миллисекунды{|}            - Пауза
{delay}миллисекунды{|}            - Задержка
{interval}интервал функция{|}     - Интервал
{now}{|}                          - Текущее время
{today}{|}                        - Сегодня
{yesterday}{|}                    - Вчера
{tomorrow}{|}                     - Завтра
{timestamp}{|}                    - Метка времени
{strftime}формат{|}               - Форматирование времени
{strptime}строка формат{|}        - Парсинг времени
{calendar}год месяц{|}            - Календарь
{timezone}зона{|}                 - Часовой пояс
{duration}секунды{|}              - Длительность
{stopwatch}{|}                    - Секундомер
{countdown}секунды{|}             - Обратный отсчёт
{alarm}время{|}                   - Будильник
```

### 17. 📝 СТРОКИ И ТЕКСТ (100+)
```
{concat}строка1 строка2{|}        - Конкатенация
{split}строка разделитель{|}      - Разделение строки
{joinstr}массив разделитель{|}    - Объединение массива
{replace}строка старое новое{|}   - Замена подстроки
{find}подстрока строка{|}         - Поиск подстроки
{substr}строка начало длина{|}    - Подстрока
{trim}строка{|}                   - Удаление пробелов
{upper}строка{|}                  - В верхний регистр
{lower}строка{|}                  - В нижний регистр
{format}шаблон аргументы{|}       - Форматирование
{escape}строка{|}                 - Экранирование
{unescape}строка{|}               - Удаление экранирования
{encode}строка кодировка{|}       - Кодирование
{decode}строка кодировка{|}       - Декодирование
{base64}строка{|}                 - Base64
{urlencode}строка{|}              - URL кодирование
{urldecode}строка{|}              - URL декодирование
{regex}паттерн строка{|}          - Регулярные выражения
{match}паттерн строка{|}          - Совпадение
{search}паттерн строка{|}         - Поиск по паттерну
```

### 18. 🧩 РАБОТА С КОЛЛЕКЦИЯМИ (100+)
```
{map}функция коллекция{|}         - Применение функции
{filter}функция коллекция{|}      - Фильтрация
{reduce}функция коллекция{|}      - Свёртка
{sort}коллекция{|}                - Сортировка
{reverse}коллекция{|}             - Реверс
{shuffle}коллекция{|}             - Перемешивание
{count}коллекция{|}               - Подсчёт
{sum}коллекция{|}                 - Сумма
{avg}коллекция{|}                 - Среднее
{min}коллекция{|}                 - Минимум
{max}коллекция{|}                 - Максимум
{first}коллекция{|}               - Первый элемент
{last}коллекция{|}                - Последний элемент
{nth}индекс коллекция{|}          - N-ный элемент
{slice}начало конец коллекция{|}  - Срез
{zip}коллекция1 коллекция2{|}     - Объединение
{enumerate}коллекция{|}           - Нумерация
{groupby}ключ коллекция{|}        - Группировка
{unique}коллекция{|}              - Уникальные значения
{flatten}коллекция{|}             - Выравнивание
```

### 19. 🐛 ОШИБКИ И ОТЛАДКА (50+)
```
{try}блок{|}                      - Попытка
{catch}ошибка блок{|}             - Перехват ошибки
{throw}ошибка{|}                  - Генерация ошибки
{error}сообщение{|}               - Создание ошибки
{debug}значение{|}                - Отладочный вывод
{log}сообщение{|}                 - Логирование
{trace}метка{|}                   - Трассировка
{assert}условие сообщение{|}      - Утверждение
{breakpoint}{|}                   - Точка останова
{watch}переменная{|}              - Наблюдение
{profile}функция{|}               - Профилирование
{benchmark}функция{|}             - Бенчмарк
{test}функция{|}                  - Тестирование
{coverage}код{|}                  - Покрытие кода
{lint}код{|}                      - Проверка стиля
{format}код{|}                    - Форматирование кода
{validate}данные схема{|}         - Валидация
{verify}код{|}                    - Верификация
{inspect}объект{|}                - Инспекция
{monitor}ресурс{|}                - Мониторинг
```

### 20. 📦 МОДУЛИ И ИМПОРТ (50+)
```
{import}модуль{|}                 - Импорт модуля
{export}элемент{|}                - Экспорт элемента
{module}имя{|}                    - Объявление модуля
{package}имя{|}                   - Пакет
{require}модуль{|}                - Требование модуля
{include}файл{|}                  - Включение файла
{using}пространство{|}            - Использование пространства имён
{namespace}имя{|}                 - Пространство имён
{library}имя{|}                   - Библиотека
{framework}имя{|}                 - Фреймворк
{plugin}имя{|}                    - Плагин
{extension}имя{|}                 - Расширение
{dependency}имя версия{|}         - Зависимость
{version}номер{|}                 - Версия
{license}тип{|}                   - Лицензия
{author}имя{|}                    - Автор
{description}текст{|}             - Описание
{repository}url{|}                - Репозиторий
{documentation}путь{|}            - Документация
{example}код{|}                   - Пример
```

## 🚀 ПРИМЕРЫ ПРОГРАММ

### Пример 1: Простая программа
```
{progname}HelloWorld{|}
{str}#message = "Привет, мир!"{|}
{echo}#message{|}
{input}#name{|}
{echo}"Привет, " #name{|}
```

### Пример 2: Калькулятор
```
{progname}Calculator{|}
{int}#a = 10{|}
{int}#b = 5{|}
{echo}"Сумма: " {add}#a #b{|}
{echo}"Разность: " {sub}#a #b{|}
{echo}"Произведение: " {mul}#a #b{|}
{echo}"Частное: " {div}#a #b{|}
```

### Пример 3: Игра
```
{gamename}SpaceShooter{|}
{window}800 600 "Космический шутер"{|}
{sprite}"spaceship.png" 400 300{|}
{animate}#spaceship 10{|}
{keyboard}"SPACE" {shoot}{|}
{mouse}"LEFT" {move}{|}
{game}#score = 0{|}
{reg}#lives = 3{|}
```

## ⚙️ КОМПИЛЯЦИЯ

### С ассемблером (рекомендуется)
```
AeroLang.exe программа.aer
```

### Без ассемблера (C фолбэк)
```
AeroLang.exe программа.aer -c
```

### Опции компиляции
```
-nasm          Использовать NASM
-masm          Использовать MASM  
-c             Использовать C фолбэк
-o имя         Имя выходного файла
-optimize      Оптимизация кода
-debug         Отладочная информация
```

## 🎨 РАСШИРЕНИЕ ДЛЯ VISUAL STUDIO

### Установка
1. Скачать `aerolang.vsix`
2. Запустить установку
3. Перезапустить Visual Studio

### Возможности
- Подсветка синтаксиса Aero
- IntelliSense для тысяч команд
- Шаблоны проектов
- Отладчик
- Интеграция с компилятором
- Иконка файлов .aer

## 📞 ПОДДЕРЖКА

### При отсутствии ассемблера
Программа предложит:
1. Указать путь к ассемблеру
2. Скачать автоматически
3. Использовать C фолбэк

### Сообщение:
"Брат, братишка, братунчик, у тебя ассемблера нет, хочешь указать путь или скачать?"

## 📊 СТАТИСТИКА
- **Всего команд:** 2000+
- **Категорий:** 20
- **Примеров:** 100+
- **Поддерживаемых платформ:** Windows, Linux, macOS
- **Типы компиляции:** ASM (NASM/MASM), C фолбэк

---
*© 2026 Aero Language Project. Все права защищены.*


## 📖 ПОДРОБНЫЕ ПРИМЕРЫ: ЧТО ПИСАТЬ ВНУТРИ КОМАНД

### 🎯 БАЗОВЫЕ КОМАНДЫ
```
{echo}Привет, мир!{|}                    ← Текст для вывода
{echo}#переменная{|}                     ← Имя переменной с #
{print}Результат: {|}                    ← Текст без перевода строки
{input}#имя{|}                           ← Имя переменной для сохранения ввода
{clear}{|}                               ← Ничего не пишем
{pause}{|}                               ← Ничего не пишем
{exit}0{|}                               ← Код выхода (0-255)
{cls}{|}                                 ← Ничего не пишем
{type}Текст без перевода строки{|}       ← Текст для вывода
{println}Текст с переводом строки{|}     ← Текст для вывода
{error}Ошибка: файл не найден{|}         ← Сообщение об ошибке
{warning}Внимание: память заканчивается{|} ← Предупреждение
{info}Программа запущена{|}              ← Информационное сообщение
{debug}Значение: #переменная{|}          ← Данные для отладки
{trace}main_function{|}                  ← Метка для трассировки
{assert}#x > 0{|}                        ← Условие для проверки
```

### 📊 ПЕРЕМЕННЫЕ
```
{int}#age = 25{|}                        ← Имя = целое число
{str}#name = "Иван"{|}                   ← Имя = текст в кавычках
{float}#pi = 3.14159{|}                  ← Имя = число с точкой
{bool}#active = true{|}                  ← Имя = true/false
{array}#numbers = [1,2,3,4,5]{|}         ← Имя = массив в []
{dict}#user = {name:"Иван",age:25}{|}    ← Имя = словарь в {}
{set}#unique = {1,2,3,4,5}{|}            ← Имя = множество в {}
{null}#temp{|}                           ← Имя переменной
{const}#MAX_SIZE = 100{|}                ← Имя = значение
{global}#counter = 0{|}                  ← Имя = значение
{local}#temp = 10{|}                     ← Имя = значение
{static}#count = 0{|}                    ← Имя = значение
{var}#x = 5{|}                           ← Имя = значение
{let}#y = 10{|}                          ← Имя = значение
```

### 🧮 МАТЕМАТИКА
```
{add}#a #b{|}                            ← Переменная1 переменная2
{add}5 10{|}                             ← Число1 число2
{sub}#total #discount{|}                 ← Уменьшаемое вычитаемое
{mul}#price #quantity{|}                 ← Множитель1 множитель2
{div}#sum #count{|}                      ← Делимое делитель
{mod}#number 2{|}                        ← Число делитель
{pow}#base #exponent{|}                  ← Основание степень
{sqrt}25{|}                              ← Число
{sin}90{|}                               ← Угол в градусах
{cos}45{|}                               ← Угол в градусах
{tan}30{|}                               ← Угол в градусах
{abs}-10{|}                              ← Число
{round}3.14159{|}                        ← Число
{floor}3.9{|}                            ← Число
{ceil}3.1{|}                             ← Число
{log}100{|}                              ← Число
{exp}2{|}                                ← Число
{random}1 100{|}                         ← Минимум максимум
{pi}{|}                                  ← Ничего не пишем
{e}{|}                                   ← Ничего не пишем
```

### 🧠 ЛОГИКА
```
{if}#age >= 18{|}                        ← Условие
  {echo}"Совершеннолетний"{|}
{|}
{else}{|}                                ← Ничего не пишем
  {echo}"Несовершеннолетний"{|}
{|}
{elif}#age >= 13{|}                      ← Условие
  {echo}"Подросток"{|}
{|}
{switch}#day{|}                          ← Значение для сравнения
  {case}"понедельник"{|}                 ← Значение для case
    {echo}"Начало недели"{|}
  {|}
  {case}"пятница"{|}                     ← Значение для case
    {echo}"Конец недели"{|}
  {|}
  {default}{|}                           ← Ничего не пишем
    {echo}"Другой день"{|}
  {|}
{|}
{and}#x > 0 #x < 10{|}                   ← Условие1 условие2
{or}#error #timeout{|}                   ← Условие1 условие2
{not}#finished{|}                        ← Условие
{eq}#a #b{|}                             ← Значение1 значение2
{neq}#old #new{|}                        ← Значение1 значение2
{gt}#score #highscore{|}                 ← Значение1 значение2
{lt}#temp 100{|}                         ← Значение1 значение2
{gte}#age 18{|}                          ← Значение1 значение2
{lte}#count 100{|}                       ← Значение1 значение2
{xor}#a #b{|}                            ← Значение1 значение2
{nand}#a #b{|}                           ← Значение1 значение2
{nor}#a #b{|}                            ← Значение1 значение2
```

### 🔄 ЦИКЛЫ
```
{for}#i = 0; #i < 10; #i++{|}            ← Инициализация; условие; шаг
  {echo}#i{|}
{|}
{while}#count > 0{|}                     ← Условие
  {echo}#count{|}
  {sub}#count 1{|}
{|}
{dowhile}#choice != "exit"{|}            ← Условие
  {input}#choice{|}
{|}
{foreach}#item in #array{|}              ← Элемент in коллекция
  {echo}#item{|}
{|}
{break}{|}                               ← Ничего не пишем
{continue}{|}                            ← Ничего не пишем
{loop}10{|}                              ← Количество повторений
  {echo}"Повтор"{|}
{|}
{repeat}5{|}                             ← Количество повторений
  {echo}"Ещё раз"{|}
{|}
{iterate}#users{|}                       ← Коллекция
  {echo}#user.name{|}
{|}
{range}1 10 2{|}                         ← Начало конец шаг
```

### 🏗️ ФУНКЦИИ
```
{func}calculate(#a, #b){|}               ← Имя(параметры)
  {add}#a #b{|}
  {return}#result{|}
{|}
{return}#value{|}                        ← Значение для возврата
{call}calculate(5, 10){|}                ← Имя(аргументы)
{param}#x 10{|}                          ← Имя значение
{arg}0{|}                                ← Индекс аргумента
{lambda}(#x, #y) => {add}#x #y{|}        ← (параметры) => выражение
{closure}#x, #y{|}                       ← Список переменных
{callback}#handler{|}                    ← Имя функции
{recursive}factorial{|}                  ← Имя функции
{generator}numbers{|}                    ← Имя фу��кции
{async}fetchData{|}                      ← Имя функции
{await}#promise{|}                       ← Выражение
{promise}#data{|}                        ← Значение
```

### 📁 ФАЙЛЫ
```
{file}"data.txt" "r"{|}                  ← Путь режим
{read}"config.json"{|}                   ← Путь к файлу
{write}"log.txt" "Запись"{|}             ← Путь данные
{append}"log.txt" "Новая запись"{|}      ← Путь данные
{delete}"temp.txt"{|}                    ← Путь к файлу
{copy}"source.txt" "backup.txt"{|}       ← Источник цель
{move}"old.txt" "new.txt"{|}             ← Источник цель
{rename}"file1.txt" "file2.txt"{|}       ← Старое новое
{mkdir}"папка"{|}                        ← Имя папки
{rmdir}"temp"{|}                         ← Имя папки
{cd}"C:\Users"{|}                        ← Путь
{ls}"."{|}                               ← Путь
{dir}"C:\"{|}                            ← Путь
{pwd}{|}                                 ← Ничего не пишем
{find}"*.txt"{|}                         ← Паттерн
{grep}"error" "log.txt"{|}               ← Текст файл
{cat}"readme.txt"{|}                     ← Путь к файлу
{head}"bigfile.txt" 10{|}                ← Путь количество строк
{tail}"log.txt" 20{|}                    ← Путь количество строк
```

### ⚙️ ПРОЦЕССЫ
```
{run}"notepad.exe"{|}                    ← Команда
{exec}"calc.exe"{|}                      ← Имя программы
{shell}"ls -la"{|}                       ← Команда shell
{cmd}"dir"{|}                            ← Команда CMD
{powershell}"Get-Process"{|}             ← Скрипт PowerShell
{kill}1234{|}                            ← PID процесса
{pid}"notepad"{|}                        ← Имя процесса
{ps}{|}                                  ← Ничего не пишем
{net}"ping google.com"{|}                ← Сетевая операция
{http}"GET https://api.example.com"{|}   ← Метод URL
{socket}"localhost" 8080{|}              ← Хост порт
{connect}"192.168.1.1" 80{|}             ← Хост порт
{listen}8080{|}                          ← Порт
{send}"Hello, server!"{|}                ← Данные
{recv}{|}                                ← Ничего не пишем
{ping}"google.com"{|}                    ← Хост
{traceroute}"example.com"{|}             ← Хост
{curl}"https://example.com"{|}           ← URL
{wget}"http://example.com/file.zip"{|}   ← URL
```

### 🎨 ГРАФИКА
```
{window}800 600 "Моя программа"{|}       ← Ширина высота название
{button}"Нажми меня" 100 200{|}          ← Текст X Y
{label}"Имя:" 50 50{|}                   ← Текст X Y
{textbox}"Введите текст" 100 150{|}      ← Текст X Y
{canvas}400 300{|}                       ← Ширина высота
{draw}"line 0,0 100,100"{|}              ← Команда рисования
{line}50 50 200 200{|}                   ← X1 Y1 X2 Y2
{rect}100 100 200 150{|}                 ← X Y ширина высота
{circle}300 200 50{|}                    ← X Y радиус
{image}"photo.jpg" 0 0{|}                ← Путь X Y
{color}255 0 0{|}                        ← R G B
{font}"Arial" 14{|}                      ← Имя размер
{menu}"Файл,Правка,Вид"{|}               ← Пункты через запятую
{toolbar}"Новый,Открыть,Сохранить"{|}    ← Кнопки через запятую
{statusbar}"Готово"{|}                   ← Текст
{scrollbar}"vertical"{|}                 ← Ориентация
{slider}0 100{|}                         ← Минимум максимум
{checkbox}"Согласен"{|}                  ← Текст
{radiobutton}"Мужской"{|}                ← Текст
{listbox}"Яблоко,Банан,Апельсин"{|}      ← Элементы через запятую
{combobox}"Красный,Зеленый,Синий"{|}     ← Элементы через запятую
{treeview}"Корень/Ветка1/Ветка2"{|}      ← Узлы через /
{table}"Имя,Возраст,Город" 10{|}         ← Столбцы количество строк
```

### 🎮 ИГРЫ
```
{game}"Моя игра" 1920 1080{|}            ← Название ширина высота
{sprite}"player.png" 400 300{|}          ← Путь X Y
{animate}#player 10{|}                   ← Спрайт количество кадров
{sound}"shot.wav"{|}                     ← Путь к файлу
{music}"background.mp3"{|}               ← Путь к файлу
{play}#music{|}                          ← Медиа-объект
{stop}#sound{|}                          ← Медиа-объект
{volume}80{|}                            ← Уровень (0-100)
{keyboard}"SPACE"{|}                     ← Код клавиши
{mouse}"LEFT" 100 200{|}                 ← Кнопка X Y
{joystick}"AXIS1" "BUTTON1"{|}           ← Ось кнопка
{camera}0 0 10{|}                        ← X Y Z
{light}"DIRECTIONAL" 0 0 -1{|}           ← Тип X Y Z
{texture}"wall.jpg"{|}                   ← Путь к файлу
{model}"character.obj"{|}                ← Путь к файлу
{particle}"FIRE" 100{|}                  ← Тип количество
{physics}#player{|}                      ← Объект
{collision}#player #enemy{|}             ← Объект1 объект2
{ai}"PATROL"{|}                          ← Поведение
{pathfinding}0,0 100,100{|}              ← Начало конец
```

### 🗄️ БАЗЫ ДАННЫХ
```
{db}"sqlite" "database.db"{|}            ← Тип подключение
{sql}"SELECT * FROM users"{|}            ← SQL запрос
{select}"name, age" "users"{|}           ← Поля таблица
{insert}"users" "Иван,25"{|}             ← Таблица значения
{update}"users" "age=26"{|}              ← Таблица значения
{delete}"users" "id=1"{|}                ← Таблица условие
{where}"age > 18"{|}                     ← Условие
{join}"users" "orders"{|}                ← Таблица1 таблица2
{table}"users" "id INT, name TEXT"{|}    ← Имя столбцы
{index}"users" "name"{|}                 ← Таблица столбец
{transaction}{|}                         ← Ничего не пишем
{commit}{|}                              ← Ничего не пишем
{rollback}{|}                            ← Ничего не пишем
{query}"SELECT COUNT(*) FROM users"{|}   ← Запрос
{fetch}#result{|}                        ← Результат запроса
{row}0{|}                                ← Индекс строки
{column}"name"{|}                        ← Имя столбца
```

### 🌐 ВЕБ
```
{web}"localhost" 8080{|}                 ← Адрес порт
{html}"<h1>Привет</h1>"{|}               ← HTML код
{css}"body { color: red; }"{|}           ← CSS стили
{js}"console.log('Hello')"{|}            ← JavaScript код
{api}"/users"{|}                         ← Эндпоинт
{json}'{"name":"Иван","age":25}'{|}      ← JSON данные
{xml}"<user><name>Иван</name></user>"{|} ← XML данные
{rest}"GET /api/users"{|}                ← Метод URL
{get}"https://api.example.com/users"{|}  ← URL
{post}"https://api.example.com/users" '{"name":"Иван"}'{|} ← URL данные
{put}"https://api.example.com/users/1" '{"name":"Петр"}'{|} ← URL данные
{patch}"https://api.example.com/users/1" '{"age":26}'{|} ← URL данные
{delete}"https://api.example.com/users/1"{|} ← URL
{header}"Authorization" "Bearer token"{|} ← Ключ значение
{cookie}"session" "abc123"{|}            ← Имя значение
{session}'{"user":"admin"}'{|}           ← Данные сессии
{auth}"admin" "password123"{|}           ← Логин пароль
{oauth}"google"{|}                       ← Провайдер
{jwt}"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9"{|} ← Токен
{websocket}"ws://localhost:8080"{|}      ← URL
```

### 🔐 БЕЗОПАСНОСТЬ
```
{encrypt}"секрет" "ключ123"{|}           ← Данные ключ
{decrypt}"зашифрованный_текст" "ключ123"{|} ← Данные ключ
{hash}"пароль" "sha256"{|}               ← Данные алгоритм
{sign}"документ" "private_key"{|}        ← Данные ключ
{verify}"документ" "подпись" "public_key"{|} ← Данные подпись ключ
{auth}"admin" "password"{|}              ← Логин пароль
{login}"user123" "pass456"{|}            ← Учётные данные
{logout}{|}                              ← Ничего не пишем
{token}"access_token"{|}                 ← Данные токена
{password}"MyPass123!"{|}                ← Пароль
{key}"AES" 256{|}                        ← Тип длина
{certificate}"server.crt"{|}             ← Путь к файлу
{ssl}"server.crt" "server.key"{|}        ← Сертификат ключ
{tls}"1.2"{|}                            ← Версия
{firewall}"block 192.168.1.100"{|}       ← Правило
{vpn}"myvpn"{|}                          ← Название подключения
{antivirus}"scan C:\"{|}                 ← Путь для сканирования
{backup}#data{|}                         ← Данные для резервирования
{recovery}#backup{|}                     ← Данные для восстановления
```

### ⚡ ПАРАЛЛЕЛИЗМ
```
{thread}processData{|}                   ← Имя функции
{async}fetchData{|}                      ← Имя функции
{await}#promise{|}                       ← Выражение
{promise}#result{|}                      ← Значение
{mutex}"db_access"{|}                    ← Имя мьютекса
{semaphore}5{|}                          ← Начальное значение
{lock}#resource{|}                       ← Ресурс
{unlock}#resource{|}                     ← Ресурс
{parallel}"task1,task2,task3"{|}         ← Задачи через запятую
{concurrent}4{|}                         ← Количество потоков
{sync}#data{|}                           ← Операция
{race}"promise1,promise2"{|}             ← Промисы через запятую
{all}"promise1,promise2,promise3"{|}     ← Промисы через запятую
{any}"promise1,promise2"{|}              ← Промисы через запятую
{pool}10{|}                              ← Размер пула
{queue}"job1,job2,job3"{|}               ← Задачи через запятую
{channel}100{|}                          ← Размер буфера
{worker}"process.js"{|}                  ← Путь к скрипту
{cluster}4{|}                            ← Количество процессов
{fork}"child_process"{|}                 ← Имя процесса
```

### 💻 СИСТЕМНОЕ
```
{mem}0x1000 1024{|}                      ← Адрес размер
{ptr}0x7FFD1234{|}                       ← Адрес
{alloc}4096{|}                           ← Размер в байтах
{free}0x1000{|}                          ← Адрес
{asm}"mov eax, 1"{|}                     ← Ассемблерный код
{reg}"eax" 10{|}                         ← Регистр значение
{interrupt}0x21{|}                       ← Номер прерывания
{port}0x60 0xFF{|}                       ← Номер значение
{bios}0x10{|}                            ← Номер функции
{kernel}"sys_read"{|}                    ← Имя функции
{driver}"network"{|}                     ← Имя устройства
{device}"keyboard"{|}                    ← Имя устройства
{io}"read" "keyboard"{|}                 ← Операция устройство
{dma}1{|}                                ← Номер канала
{irq}1{|}                               ← Номер прерывания
{cpu}"hlt"{|}                           ← Инструкция
{gpu}"draw_triangle"{|}                  ← Команда
{mmu}"page_table"{|}                    ← Имя таблицы
{cache}"flush"{|}                       ← Операция
{pipeline}"fetch,decode,execute"{|}     ← Стадии через запятую
```

### 📅 ДАТА И ВРЕМЯ
```
{date}"%d.%m.%Y"{|}                      ← Формат
{time}"%H:%M:%S"{|}                      ← Формат
{datetime}"%d.%m.%Y %H:%M:%S"{|}         ← Формат
{timer}1000{|}                           ← Интервал в мс
{sleep}500{|}                            ← Миллисекунды
{delay}1000{|}                           ← Миллисекунды
{interval}60 #update{|}                  ← Интервал функция
{now}{|}                                 ← Ничего не пишем
{today}{|}                               ← Ничего не пишем
{yesterday}{|}                           ← Ничего не пишем
{tomorrow}{|}                            ← Ничего не пишем
{timestamp}{|}                           ← Ничего не пишем
{strftime}"%A, %d %B %Y"{|}              ← Формат
{strptime}"2024-12-31" "%Y-%m-%d"{|}     ← Строка формат
{calendar}2024 12{|}                     ← Год месяц
{timezone}"Europe/Moscow"{|}             ← Название зоны
{duration}3600{|}                        ← Секунды
{stopwatch}{|}                           ← Ничего не пишем
{countdown}10{|}                         ← Секунды
{alarm}"08:00"{|}                        ← Время
```

### 📝 СТРОКИ
```
{concat}"Привет, " "мир!"{|}             ← Строка1 строка2
{split}"яблоко,банан,апельсин" ","{|}    ← Строка разделитель
{joinstr}#array ","{|}                   ← Массив разделитель
{replace}"Привет, Иван" "Иван" "Петр"{|} ← Строка старое новое
{find}"мир" "Привет, мир!"{|}            ← Подстрока строка
{substr}"Привет, мир!" 0 7{|}            ← Строка начало длина
{trim}"  текст  "{|}                     ← Строка
{upper}"привет"{|}                       ← Строка
{lower}"ПРИВЕТ"{|}                       ← Строка
{format}"Привет, %s!" "Иван"{|}          ← Шаблон аргументы
{escape}"Текст с "кавычками""{|}         ← Строка
{unescape}"Текст с \"кавычками\""{|}     ← Строка
{encode}"Привет" "utf-8"{|}              ← Строка кодировка
{decode}"0x41 0x42 0x43" "ascii"{|}      ← Строка кодировка
{base64}"SGVsbG8gV29ybGQ="{|}            ← Строка
{urlencode}"Привет мир!"{|}              ← Строка
{urldecode}"%D0%9F%D1%80%D0%B8%D0%B2%D0%B5%D1%82"{|} ← Строка
{regex}"\\d+" "abc123def"{|}             ← Паттерн строка
{match}"\\d+" "abc123def"{|}             ← Паттерн строка
{search}"мир" "Привет, мир!"{|}          ← Паттерн строка
```

### 🧩 КОЛЛЕКЦИИ
```
{map}#double #numbers{|}                 ← Функция коллекция
{filter}#even #numbers{|}                ← Функция коллекция
{reduce}#sum #numbers{|}                 ← Функция коллекция
{sort}#numbers{|}                        ← Коллекция
{reverse}#array{|}                       ← Коллекция
{shuffle}#cards{|}                       ← Коллекция
{count}#items{|}                         ← Коллекция
{sum}#numbers{|}                         ← Коллекция
{avg}#scores{|}                          ← Коллекция
{min}#values{|}                          ← Коллекция
{max}#values{|}                          ← Коллекция
{first}#list{|}                          ← Коллекция
{last}#list{|}                           ← Коллекция
{nth}2 #array{|}                         ← Индекс коллекция
{slice}2 5 #array{|}                     ← Начало конец коллекция
{zip}#names #ages{|}                     ← Коллекция1 коллекция2
{enumerate}#items{|}                     ← Коллекция
{groupby}"category" #products{|}         ← Ключ коллекция
{unique}#numbers{|}                      ← Коллекция
{flatten}#nested_array{|}                ← Коллекция
```

### 🐛 ОТЛАДКА
```
{try}{|}                                 ← Ничего не пишем
  {read}"file.txt"{|}
{|}{catch}#error{|}                      ← Имя ошибки
  {echo}"Ошибка: " #error{|}
{|}
{throw}"Файл не найден"{|}               ← Сообщение об ошибке
{error}"Деление на ноль"{|}              ← Сообщение об ошибке
{debug}#variable{|}                      ← Значение для отладки
{log}"Пользователь вошёл"{|}             ← Сообщение для лога
{trace}"calculate"{|}                    ← Метка для трассировки
{assert}#x != 0 "x не должен быть 0"{|}  ← Условие сообщение
{breakpoint}{|}                          ← Ничего не пишем
{watch}#counter{|}                       ← Имя переменной
{profile}#function{|}                    ← Имя функции
{benchmark}#algorithm{|}                 ← Имя алгоритма
{test}#module{|}                         ← Имя модуля
{coverage}"src/"{|}                      ← Путь к коду
{lint}"main.aer"{|}                      ← Путь к файлу
{format}"code.aer"{|}                    ← Путь к файлу
{validate}#data #schema{|}               ← Данные схема
{verify}"program.exe"{|}                 ← Путь к файлу
{inspect}#object{|}                      ← Объект
{monitor}#memory{|}                      ← Ресурс
```

### 📦 МОДУЛИ
```
{import}"math"{|}                        ← Имя модуля
{export}#function{|}                     ← Имя элемента
{module}"utils"{|}                       ← Имя модуля
{package}"com.example"{|}                ← Имя пакета
{require}"fs"{|}                         ← Имя модуля
{include}"header.aer"{|}                 ← Путь к файлу
{using}"System"{|}                       ← Пространство имён
{namespace}"MyApp"{|}                    ← Имя пространства
{library}"libcurl"{|}                    ← Имя библиотеки
{framework}"React"{|}                    ← Имя фреймворка
{plugin}"image_processor"{|}             ← Имя плагина
{extension}"vscode-aero"{|}              ← Имя расширения
{dependency}"express" "4.18.0"{|}        ← Имя версия
{version}"2.0.0"{|}                      ← Номер версии
{license}"MIT"{|}                        ← Тип лицензии
{author}"Иван Иванов"{|}                 ← Имя автора
{description}"Моя программа"{|}          ← Текст описания
{repository}"https://github.com/user/repo"{|} ← URL репозитория
{documentation}"docs/"{|}                ← Путь к документации
{example}"example.aer"{|}                ← Путь к примеру
```

## 🎯 КАК ИСПОЛЬЗОВАТЬ ЭТОТ СПРАВОЧНИК

1. **Найди нужную команду** в категории
2. **Посмотри пример** что писать внутри `{ }`
3. **Добавь закрывающую скобку** `{|}`
4. **Используй переменные** с `#` перед именем
5. **Строки** всегда в кавычках `"текст"`
6. **Числа** без кавычек `123`
7. **Массивы** в квадратных скобках `[1,2,3]`
8. **Объекты** в фигурных скобках `{ключ:значение}`

## 📦 VSIX РАСШИРЕНИЕ ДЛЯ VS CODE

### Установка:
1. **Скачай VSIX файл**: `aerolang-2.0.0.vsix`
2. **Установи в VS Code**: 
   ```
   code --install-extension aerolang-2.0.0.vsix
   ```
3. **Или вручную**: 
   - Скопируй папку `aerolang-extension` в `%USERPROFILE%\.vscode\extensions\`

### Файлы расширения:
- `package.json` - Манифест расширения
- `syntaxes/aero.tmLanguage.json` - Подсветка синтаксиса для 2000+ команд
- `language-configuration.json` - Конфигурация языка
- `snippets.json` - Шаблоны кода
- `extension.js` - Основной код расширения
- `icon.svg` - Иконка языка

### Команды в VS Code:
- `Ctrl+Shift+P` → "Aero: Compile Current File"
- `Ctrl+Shift+P` → "Aero: Show Command Reference"
- `Ctrl+Shift+P` → "Aero: Run Compiled Program"

## 🚀 БЫСТРЫЙ СТАРТ

1. **Установи расширение** (VSIX файл)
2. **Создай файл** `program.aer`
3. **Напиши код**:
   ```aer
   {progname}MyProgram{|}
   {str}#name = "Иван"{|}
   {echo}"Привет, " #name{|}
   ```
4. **Скомпилируй**: `AeroLang.exe program.aer`
5. **Запусти**: `program.exe`

## 📞 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

1. **Проверь синтаксис**: `{команда}параметры{|}`
2. **Убедись в закрывающей скобке**: `{|}`
3. **Переменные начинаются с `#`**: `#name`
4. **Строки в кавычках**: `"текст"`
5. **Если нет ассемблера** - используй `-c`:
   ```
   AeroLang.exe program.aer -c
   ```

## 🎨 ИКОНКИ ДЛЯ ЯЗЫКА

В папке проекта есть 4 иконки:
1. `AeroLang.svg` - Основная (космическая тема)
2. `aerolang_icon1.svg` - Простая с буквой A
3. `aerolang_icon2.svg` - С треугольником
4. `aerolang_icon3.svg` - С шестерёнкой

**Используй любую** для своего проекта или расширения.

---

*© 2026 Aero Language Project. 2000+ команд, мощь Assembly, простота Python.*
*Синтаксис: `{команда}параметры{|}` - запомни закрывающую скобку!*


## 🎨 РАСШИРЕННАЯ МАТЕМАТИКА: 3D, ЦВЕТА, ВЕКТОРЫ

### 🌈 ЦВЕТА И ГРАДИЕНТЫ
```
{rgb}r g b{|}                       - Цвет RGB (0-255)
{rgba}r g b a{|}                    - Цвет RGBA с прозрачностью
{hsl}h s l{|}                      - Цвет HSL (0-360, 0-100%, 0-100%)
{hsla}h s l a{|}                   - Цвет HSLA с прозрачностью
{hex}"#FF0000"{|}                  - Цвет в HEX формате
{gradient}"linear" #colors{|}      - Градиент (linear/radial)
{blend}#color1 #color2 0.5{|}      - Смешивание цветов (0-1)
{invert}#color{|}                  - Инвертирование цвета
{brightness}#color 1.2{|}          - Изменение яркости
{contrast}#color 1.5{|}            - Изменение контрастности
{saturate}#color 0.8{|}            - Изменение насыщенности
{desaturate}#color 0.5{|}          - Уменьшение насыщенности
{grayscale}#color{|}               - Оттенки серого
{sepia}#color{|}                   - Сепия эффект
```

### 📐 3D МАТЕМАТИКА И ВЕКТОРЫ
```
{vec2}x y{|}                       - 2D вектор
{vec3}x y z{|}                     - 3D вектор
{vec4}x y z w{|}                   - 4D вектор
{mat3}a11 a12 a13 a21 a22 a23 a31 a32 a33{|} - Матрица 3x3
{mat4}...{|}                       - Матрица 4x4 (16 значений)
{quat}x y z w{|}                   - Кватернион
{length}#vector{|}                 - Длина вектора
{normalize}#vector{|}              - Нормализация вектора
{dot}#vec1 #vec2{|}                - Скалярное произведение
{cross}#vec1 #vec2{|}              - Векторное произведение
{lerp}#start #end 0.5{|}           - Линейная интерполяция
{slerp}#q1 #q2 0.5{|}              - Сферическая интерполяция
{reflect}#vector #normal{|}        - Отражение вектора
{refract}#vector #normal 1.5{|}    - Преломление вектора
{transform}#vector #matrix{|}      - Преобразование вектора матрицей
{rotate}#vector #axis 45{|}        - Вращение вектора
{scale}#vector 2.0{|}              - Масштабирование вектора
{translate}#vector 10 20 30{|}     - Перенос вектора
```

### 🎮 3D ГРАФИКА И ОБЪЕКТЫ
```
{mesh}вершины индексы{|}           - Создание меша
{vertex}x y z u v nx ny nz{|}      - Вершина 3D модели
{triangle}v1 v2 v3{|}              - Треугольник
{quad}v1 v2 v3 v4{|}               - Четырёхугольник
{cube}ширина высота глубина{|}     - Куб
{sphere}радиус сегменты{|}         - Сфера
{cylinder}радиус высота сегменты{|} - Цилиндр
{cone}радиус высота сегменты{|}    - Конус
{torus}большой_радиус малый_радиус{|} - Тор
{plane}ширина высота сегменты{|}   - Плоскость
{pyramid}основание высота{|}       - Пирамида
{icosphere}радиус итерации{|}      - Икосаэдр/сфера
{teapot}размер{|}                  - Чайник Юты
{obj}"model.obj"{|}                - Загрузка OBJ модели
{fbx}"model.fbx"{|}                - Загрузка FBX модели
{gltf}"scene.gltf"{|}              - Загрузка GLTF сцены
```

### 🎨 МАТЕРИАЛЫ И ТЕКСТУРЫ
```
{material}параметры{|}             - Создание материала
{diffuse}#color{|}                 - Диффузный цвет
{specular}#color интенсивность{|}  - Зеркальный цвет
{emissive}#color{|}                - Свечение
{ambient}#color{|}                 - Фоновое освещение
{shininess}32{|}                   - Блеск материала
{opacity}0.8{|}                    - Прозрачность (0-1)
{texture}"diffuse.jpg"{|}          - Диффузная текстура
{normalmap}"normal.png"{|}         - Карта нормалей
{specularmap}"specular.png"{|}    - Карта зеркальности
{displacement}"height.png"{|}     - Карта смещения
{roughness}0.3{|}                  - Шероховатость (0-1)
{metallic}0.8{|}                   - Металличность (0-1)
{ao}"ambient_occlusion.png"{|}    - Карта окружающего затенения
{pbr}параметры{|}                  - PBR материал
{shader}"vertex.frag"{|}           - Шейдер
{program}"vertex.vs" "fragment.fs"{|} - Шейдерная программа
```

### 🎥 КАМЕРЫ И ОСВЕЩЕНИЕ
```
{perspective}fov соотношение near far{|} - Перспективная камера
{orthographic}лево право низ верх near far{|} - Ортографическая камера
{lookat}глаз цель верх{|}          - Направление камеры
{orbit}радиус theta phi{|}         - Орбитальная камера
{firstperson}позиция yaw pitch{|}  - Камера от первого лица
{thirdperson}цель расстояние{|}    - Камера от третьего лица
{dirlight}направление цвет{|}      - Направленный свет
{pointlight}позиция цвет радиус{|} - Точечный свет
{spotlight}позиция направление угол цвет{|} - Прожектор
{ambientlight}цвет{|}              - Фоновое освещение
{hemilight}небо земля{|}           - Полусферический свет
{arealight}позиция размер цвет{|}  - Площадной свет
{shadow}разрешение{|}              - Тени
{ssao}радиус samples{|}            - SSAO (окружающее затенение)
{bloom}интенсивность threshold{|}  - Эффект bloom
{dof}фокус aperture{|}             - Глубина резкости
{motionblur}скорость{|}            - Размытие в движении
```

### 🎯 ФИЗИКА 3D
```
{rigidbody}масса{|}                - Твёрдое тело
{collider}"box" размеры{|}         - Коллайдер (box/sphere/capsule/mesh)
{joint}#body1 #body2{|}            - Соединение тел
{constraint}тип параметры{|}       - Ограничение
{force}#тело вектор{|}             - Применение силы
{torque}#тело вектор{|}            - Применение крутящего момента
{impulse}#тело вектор{|}           - Импульс
{gravity}9.81{|}                   - Гравитация
{friction}0.5{|}                   - Трение
{restitution}0.8{|}                - Упругость
{raycast}начало направление длина{|} - Луч для обнаружения
{sweeptest}#collider движение{|}   - Проверка перемещения
{trigger}#collider{|}              - Триггер (без столкновений)
{charactercontroller}рост радиус{|} - Контроллер персонажа
{vehicle}масса колеса{|}           - Транспортное средство
{fluid}плотность вязкость{|}       - Жидкость
{cloth}размеры constraints{|}      - Ткань
{softbody}вершины springs{|}       - Мягкое тело
{ragdoll}кости constraints{|}      - Тряпичная кукла
```

### 🎨 ПОСТОБРАБОТКА И ЭФФЕКТЫ
```
{postprocess}эффекты{|}            - Постобработка
{antialiasing}"MSAA" 4{|}          - Сглаживание
{tonemapping}"ACES"{|}             - Тонмаппинг
{colorcorrection}lut{|}            - Коррекция цвета
{vignette}интенсивность{|}         - Виньетирование
{chromatic}сила{|}                 - Хроматическая аберрация
{film grain}сила{|}                - Зернистость плёнки
{lensflare}элементы{|}             - Блики объектива
{volumetric}плотность{|}           - Объёмный свет
{godray}источник интенсивность{|}  - Лучи бога
{reflection}качество{|}            - Отражения
{refraction}индекс{|}              - Преломление
{subsurface}толщина цвет{|}        - Подповерхностное рассеивание
{parallax}высота шаги{|}           - Параллакс-окклюзия
{tessellation}фактор{|}            - Тесселяция
{displacement}карта сила{|}        - Смещение вершин
{decal}текстура позиция{|}         - Декаль (наклейка)
{particlesystem}параметры{|}       - Система частиц
{trail}длина цвет{|}               - След
{ribbon}ширина цвет{|}             - Лента
```

### 🧮 МАТЕМАТИКА ДЛЯ 3D И ГРАФИКИ
```
{radians}градусы{|}                - Градусы в радианы
{degrees}радианы{|}                - Радианы в градусы
{clamp}значение мин макс{|}        - Ограничение значения
{smoothstep}мин макс значение{|}   - Плавный переход
{step}edge значение{|}             - Ступенчатая функция
{fract}число{|}                    - Дробная часть
{mod}число делитель{|}             - Остаток от деления (для float)
{mix}значение1 значение2 фактор{|} - Смешивание (аналог lerp)
{smoothmin}a b k{|}                - Плавный минимум
{smoothmax}a b k{|}                - Плавный максимум
{distance}точка1 точка2{|}         - Расстояние между точками
{angle}вектор1 вектор2{|}          - Угол между векторами
{project}вектор на_вектор{|}       - Проекция вектора
{reject}вектор от_вектора{|}       - Отклонение вектора
{barycentric}точка треугольник{|}  - Барицентрические координаты
{intersect}луч треугольник{|}      - Пересечение луча с треугольником
{intersectsphere}луч сфера{|}      - Пересечение луча со сферой
{intersectbox}луч aabb{|}          - Пересечение луча с AABB
{inside}точка объем{|}             - Проверка нахождения внутри
{convexhull}точки{|}               - Выпуклая оболочка
{delaunay}точки{|}                 - Триангуляция Делоне
{voronoi}точки{|}                  - Диаграмма Вороного
{noise}координаты seed{|}          - Шум (Perlin/Simplex)
{fbm}координаты octaves{|}         - Фрактальный Броуновский шум
{cellular}координаты{|}            - Клеточный шум
{worley}координаты{|}              - Шум Ворлея
```

### 🎯 ПРИМЕРЫ 3D И ЦВЕТОВ

#### Создание 3D сцены:
```aer
{game}3DScene 1920 1080{|}
{perspective}60 1.777 0.1 1000{|}
{lookat}0 0 5 0 0 0 0 1 0{|}

{pointlight}0 5 0 1 1 1 10{|}
{dirlight}0 -1 0 0.8 0.8 0.8{|}

{cube}1 1 1{|}
{material}{|}
{diffuse}1 0.5 0{|}
{specular}1 1 1 32{|}
{position}0 0 0{|}

{sphere}0.5 32{|}
{material}{|}
{rgb}0 0.7 1{|}
{metallic}0.8{|}
{roughness}0.2{|}
{position}2 0 0{|}
```

#### Работа с цветами:
```aer
{progname}ColorDemo{|}
{rgb}#red = 255 0 0{|}
{rgba}#transparentBlue = 0 0 255 0.5{|}
{hex}#green = "#00FF00"{|}
{hsl}#purple = 300 100 50{|}

{blend}#red #green 0.5{|}
{invert}#red{|}
{brightness}#blue 1.5{|}
{gradient}"linear" #red #green #blue{|}
```

#### Векторная математика:
```aer
{progname}VectorMath{|}
{vec3}#v1 = 1 2 3{|}
{vec3}#v2 = 4 5 6{|}

{length}#v1{|}
{normalize}#v1{|}
{dot}#v1 #v2{|}
{cross}#v1 #v2{|}
{lerp}#v1 #v2 0.5{|}
```

#### Создание сложного материала:
```aer
{material}#gold{|}
{diffuse}1 0.84 0{|}
{specular}1 0.94 0.8 128{|}
{metallic}1.0{|}
{roughness}0.1{|}
{texture}"gold_albedo.jpg"{|}
{normalmap}"gold_normal.png"{|}
{roughnessmap}"gold_roughness.png"{|}
```

## 🎯 КАК ИСПОЛЬЗОВАТЬ 3D И ЦВЕТА:

### Для 3D объектов:
1. **Создай геометрию**: `{cube}`, `{sphere}`, `{mesh}`
2. **Задай материал**: `{material}`, `{diffuse}`, `{specular}`
3. **Добавь текстуры**: `{texture}`, `{normalmap}`
4. **Настрой освещение**: `{pointlight}`, `{dirlight}`
5. **Управляй камерой**: `{perspective}`, `{lookat}`

### Для работы с цветами:
1. **Выбери формат**: `{rgb}`, `{rgba}`, `{hsl}`, `{hex}`
2. **Применяй эффекты**: `{blend}`, `{invert}`, `{brightness}`
3. **Создай градиенты**: `{gradient}`
4. **Используй в материалах**: `{diffuse}#color`

### Для векторной математики:
1. **Создай векторы**: `{vec2}`, `{vec3}`, `{vec4}`
2. **Выполняй операции**: `{dot}`, `{cross}`, `{length}`
3. **Преобразуй**: `{normalize}`, `{lerp}`, `{rotate}`
4. **Используй в 3D**: позиции, нормали, направления

## 🚀 БЫСТРЫЙ СТАРТ С 3D:

```aero
{gamename}My3DGame{|}
{window}1280 720 "3D Game"{|}

{perspective}75 1.777 0.1 1000{|}
{lookat}0 2 5 0 0 0 0 1 0{|}

{dirlight}1 -1 1 0.8 0.8 0.8{|}

{cube}1 1 1{|}
{material}{|}
{rgb}1 0 0{|}
{position}0 0 0{|}

{sphere}0.5 16{|}
{material}{|}
{rgb}0 0 1{|}
{position}2 0 0{|}

{keyboard}"W" {moveForward}{|}
{keyboard}"S" {moveBackward}{|}
{mouse}"MOVE" {lookAround}{|}
```

**Теперь в Aero Language есть полная поддержка 3D графики, цветов, векторной математики и физики!** 🎨🎮📐